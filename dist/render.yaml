services:
  - type: web
    name: retailedge-trading-tournament
    env: node
    buildCommand: npm install
    startCommand: node tournament-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3002
    healthCheckPath: /
